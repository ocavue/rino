{
    "name": "@rino.app/electron-preload",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "tsc --build ./src/tsconfig.json && node esbuild.mjs --watch",
        "build": "tsc --build ./src/tsconfig.json && node esbuild.mjs"
    },
    "devDependencies": {
        "@rino.app/electron-types": "workspace:0.0.0",
        "electron": "18.1.0",
        "electron-log": "^4.4.6",
        "esbuild": "^0.14.38",
        "typescript": "^4.6.3"
    }
}
