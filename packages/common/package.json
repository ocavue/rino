{
    "name": "@rino.app/common",
    "version": "0.0.0",
    "private": true,
    "license": "GPL-3.0",
    "type": "module",
    "main": "dist/index.js",
    "module": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "test:vitest": "vitest run",
        "test:vitest:dev": "vitest dev",
        "test:vitest:coverage": "vitest run --coverage",
        "dev":  "tsc --build --watch --preserveWatchOutput src/tsconfig.json",
        "build": "tsc --build src/tsconfig.json"
    },
    "publishConfig": {
        "access": "public"
    },
    "devDependencies": {
        "typescript": "^4.7.2",
        "vitest": "^0.12.10"
    }
}
