{
    "name": "visite",
    "type": "module",
    "version": "0.0.1",
    "private": true,
    "license": "MIT",
    "bin": {
        "visite": "./bin/visite.mjs"
    },
    "scripts": {
        "build": "run-s clean build:esbuild build:tsc",
        "build:esbuild": "node esbuild.mjs",
        "build:tsc": "tsc --build ./src/tsconfig.json",
        "clean": "rimraf ./dist"
    },
    "dependencies": {
        "connect": "^3.7.0",
        "react-helmet-async": "^1.3.0",
        "react-router-dom": "^6.6.1",
        "vite": "^3.2.5"
    },
    "peerDependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "@types/connect": "^3.4.35",
        "esbuild": "^0.16.12",
        "npm-run-all": "^4.1.5",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "rimraf": "^3.0.2"
    }
}
